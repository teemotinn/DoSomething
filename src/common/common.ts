import { FailureResponse } from "../connection/FailureResponse"

export const NOT_AVAILABLE_INFORMATION = 'Not available'

export const DEFAULT_API_ERROR_RESPONSE = new FailureResponse('Something went wrong!')